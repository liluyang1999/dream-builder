# 2026-08-10 · 1.0 Windows 当前用户安装生命周期

## 结论

从干净提交 `5be1ff3e90751a8f47a7e43da3330c2f675b724c` 构建的 1.0 未签名 Windows x64
候选在当前目标用户下完成了安装、同版本重装、首次启动、WebView2、单实例、正常关闭与卸载闭环。
Windows“已安装的应用”、安装器、主程序和卸载器均只显示两段式版本 1.0。卸载后没有程序集成
残留；既有玩家数据按升级与诊断策略保留，测试没有读取或删除其中内容。

这份证据证明的是下列精确未签名候选在本机当前用户下的生命周期，不把它提升为生产发布。
受信任代码签名、干净 Windows 账户覆盖升级、实体手柄完整听测和五名首次接触玩家观察仍是外部门槛。

## 精确候选

| 项目 | 实测值 |
|---|---|
| 产品版本 | `1.0` |
| 来源提交 | `5be1ff3e90751a8f47a7e43da3330c2f675b724c` |
| 源码状态 | 干净；发布清单 `SourceDirty: False` |
| 源码树 SHA-256 | `81A826BF826447B25CCE43AADC29C0EF544A2F762641D513D44BC337FA3E2080` |
| 便携 EXE | 10,230,784 bytes · `1C4EC65543169BF6923F33836D5CAE31A128898EC8EBF97DA517C3F722816BC6` |
| NSIS 安装器 | 2,565,384 bytes · `54ADBA5C63CF5218D42633BFA526CFBD67BF5123D3FBB9BE35DD1573B4300D94` |
| 签名状态 | `NotSigned`；仅作为本机候选验证 |
| WebView2 策略 | Evergreen `downloadBootstrapper`，不固定 Runtime 补丁版本 |

`pnpm release:verify` 独立复算了六文件集合、来源提交、源码树指纹、文件大小、SHA-256、许可与
手册副本、两段式文件版本和签名状态。

## 实测步骤与结果

| 阶段 | 新鲜证据 | 结果 |
|---|---|---|
| 安装前 | 卸载项、程序目录、快捷方式和游戏进程均为 0；玩家数据存在 | 通过 |
| 安装 | 静默运行上述精确 NSIS，退出码 0 | 通过 |
| Windows 版本显示 | 唯一 HKCU 卸载项的 `DisplayVersion` 为 1.0；安装器、主 EXE 与卸载器的产品版本和文件版本均为 1.0 | 通过 |
| 同版本重装 | 再次运行同一安装器，退出码 0；仍只有一个卸载项和一个程序目录 | 通过 |
| 快捷方式 | 开始菜单和当前用户桌面各存在一个产品快捷方式 | 通过 |
| 首次启动 | 从真实安装目录启动；主窗口标题为 `Dream Builder Fantasy Tree` | 通过 |
| WebView2 | 进程树确认 6 个 `msedgewebview2.exe` 后代属于该游戏进程 | 通过 |
| 单实例 | 第二次启动在 10 秒内以 0 退出；原进程继续运行，已安装游戏进程总数保持 1 | 通过 |
| 正常关闭 | 主窗口接受正常关闭请求并在 15 秒内退出；已安装游戏进程归零 | 通过 |
| 卸载 | 运行安装目录内的卸载器，退出码 0 | 通过 |
| 卸载后 | 卸载项、程序目录、快捷方式和游戏进程再次全部为 0 | 通过 |
| 数据策略 | `com.dreambuilder.fantasytree` 玩家数据仍存在，未读取、未删除 | 通过 |

## 证据边界

- 本轮用窗口标题、版本资源、进程树和系统安装状态证明原生首次启动与生命周期；没有把未执行的
  原生视觉点击流程写成已完成。浅色/高对比与模态键盘流程另由全新真实 Chromium 会话复核，控制台无错误或警告。
- 当前证书库没有可用的受信任代码签名私钥证书，因此该候选不能作为生产分发包。
- 本轮未在全新 Windows 账户或虚拟机执行旧版覆盖升级与回滚。
- 本轮未用实体手柄走完光种、记忆、净化和章节结尾，也未完成五名首次接触玩家的无提示观察。
