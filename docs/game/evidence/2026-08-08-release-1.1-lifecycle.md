# 2026-08-08—09 · 1.1 Windows 当前用户安装生命周期

## 结论

产品版本 1.1（技术版本 1.1.0）的未签名 Windows x64 候选包在当前目标 Windows 用户下完成
安装、首次启动、WebView2、单实例、正常关闭与卸载闭环。卸载后没有程序集成残留，玩家数据按
既定升级/诊断策略保留。

这份证据只证明本机当前用户生命周期，不把候选包提升为生产发布。候选包仍未使用受信任代码
签名证书，也没有替代干净 Windows 账户覆盖升级、实体手柄完整流程和五名首次接触玩家观察。

## 被测候选

- 交付目录：`output/release/1.1/`
- 便携文件：`Dream-Builder-Fantasy-Tree_1.1_x64.exe`
- 安装文件：`Dream-Builder-Fantasy-Tree_1.1_x64-setup.exe`
- 发布清单：schema 4；`version: 1.1`；`technicalVersion: 1.1.0`
- 候选状态：`releaseKind: candidate`；两份 EXE 均为 `NotSigned`
- 安装模式：当前用户 NSIS；WebView2 Evergreen `downloadBootstrapper`，不固定 Runtime 版本

## 实测步骤与结果

| 阶段 | 新鲜证据 | 结果 |
|---|---|---|
| 安装前 | 卸载项、程序目录、快捷方式、游戏进程均为 0；玩家数据存在 | 通过 |
| 安装 | 同一已校验 NSIS 以静默模式运行，进程退出码 0 | 通过 |
| 安装状态 | 唯一 HKCU 卸载项；DisplayVersion 1.1.0；安装目录与主 EXE 存在 | 通过 |
| 快捷方式 | 开始菜单和当前用户桌面各存在一个产品快捷方式 | 通过 |
| 首次启动 | 从真实安装目录启动；主窗口标题为 `Dream Builder Fantasy Tree` | 通过 |
| WebView2 | 相对启动前新增 4 个 `msedgewebview2.exe` 进程 | 通过 |
| 单实例 | 第二次启动退出码 0；原进程继续运行；游戏进程总数保持 1 | 通过 |
| 正常退出 | 主窗口接受正常关闭请求；15 秒内退出；游戏进程归零 | 通过 |
| 卸载 | 安装目录内 `uninstall.exe /S` 退出码 0 | 通过 |
| 卸载后 | 卸载项、程序目录、快捷方式、游戏进程均为 0 | 通过 |
| 数据策略 | `com.dreambuilder.fantasytree` 用户数据仍存在，未读取、未删除 | 通过 |

## 2026-08-09 兼容依赖刷新后复测

在前端兼容依赖、jsdom 30、Tauri 2.11.5 及其插件/传递依赖更新后，重新构建同版本未签名候选，
并再次执行完整生命周期。该候选构建于提交前的受控工作树，因此发布清单如实记录
`sourceDirty: true`；它用于验证更新后的二进制行为，不是最终干净来源候选。

- 源码树 SHA-256：`D7848BEADEF4BD6E0A48FEA71F03BA90AE57E57584DFDC4A6F1549D22BB62F03`
- 便携 EXE SHA-256：`AA6C44333C871B59C1A7A930C64173C2BA51AF4031C719D23A4FEDD243E488F4`
- NSIS SHA-256：`8CC64B2882A240653D2906353A541C4FD20C242F8DF835FD96BB2A1492144D22`
- 安装前状态：卸载项、程序目录、快捷方式、游戏进程均为 0；玩家数据保留；
- 安装/首次启动：安装退出码 0，DisplayVersion 1.1.0，主窗口句柄非零且标题正确，新增
  5 个 `msedgewebview2.exe` 进程；
- 单实例/退出：第二实例退出码 0，运行实例总数保持 1；正常关闭请求成功，15 秒内进程归零；
- 卸载后状态：卸载退出码 0，卸载项、程序目录、快捷方式、游戏进程再次全部为 0，玩家数据保留。

## 未覆盖范围

- 没有受信任 Authenticode 证书，因此不是签名生产包；
- 没有在全新 Windows 账户或虚拟机执行首次安装与旧版覆盖升级；
- 没有在本轮用实体手柄完整走完光种、记忆、净化和章节结尾；
- 没有完成五名首次接触玩家的无口头提示观察；
- Windows 控制插件本轮无法枚举窗口，因此首次启动以主窗口句柄/标题、进程和 WebView2 子进程
  作为系统级证据，没有把视觉点击验收写成已完成。
